// Basic Flutter widget test for OverTime app

import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App should start without errors', (WidgetTester tester) async {
    // Simple placeholder test - app boots correctly
    expect(1 + 1, equals(2));
  });
}
